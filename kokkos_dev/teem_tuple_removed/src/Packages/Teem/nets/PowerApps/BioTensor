
# This script is just a short cut to run the BioTensor SCIRun Power App
# It will be copied to the top of the object side of the tree.

if grep -q Packages/Teem __OBJ_DIR__/configVars.mk
then
   # Just loading the needed packages.
   export LOAD_PACKAGE=SCIRun,Teem

  __OBJ_DIR__/scirun __SRC_DIR__/Packages/Teem/nets/PowerApps/BioTensor.app $1
else
  echo "Your scirun was not compiled with the Teem Package.  Please"
  echo "reconfigure by adding 'Teem' to the --enable-package='package list'" 
  echo "argument to configure and then run gmake.  This will allow you to"
  echo "run BioTensor."
fi

