#!/usr/local/bin/python

from sys import argv,exit
from helpers.runSusTests import runSusTests


TESTS = [   ("regressionFMPM", "regressionFMPM.ups", 4, "ALL", ["no_restart"]), \
    	]

exit(runSusTests(argv, TESTS, "MPMF"))
