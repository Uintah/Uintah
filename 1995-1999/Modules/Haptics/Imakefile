
#include <config/sci.cf>

OBJS = Endpoint.o  Force.o  HedgehogLitLines.o  PhantomData.o  \
	Position.o  PositionToForce.o  
SRCS = Endpoint.cc Force.cc HedgehogLitLines.cc PhantomData.cc \
	Position.cc PositionToForce.cc 

LIBNAME = Haptics

SO_LIBS =
SO_LDFLAGS = -L$(TOP)/lib -lDatatypes -lGeometry -lDataflow -lGeom -lClasslib \
	-lWidgets -lMultitask -lMalloc -lm -lTCL

SCIDependTarget($(SRCS))
SCIObjLibraryTarget($(LIBNAME),$(OBJS),$(SO_LIBS),$(SO_LDFLAGS))
