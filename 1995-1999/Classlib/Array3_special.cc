
#include <Classlib/Array3.cc>

void
Array3<double>::get_onedim_byte( unsigned char *v )
{
  int i,j,k, index;
  index = 0;
  
  for( k=0; k<dm3; k++ )
    for( j=0; j<dm2; j++ )
      for( i=0; i<dm1; i++)
	v[index++] = (unsigned char)objs[i][j][k];
}

