
#include <config/sci.cf>

#ifdef SCI_DIR_Modules_Gradient

OBJS = Gradient.o
SRCS = Gradient.cc

LIBNAME = Fields

SCIDependTarget($(SRCS))
SCIObjLibraryTarget($(LIBNAME),$(OBJS))

#else
all:
depend:
#endif
