
#include <config/sci.cf>

#ifdef SCI_DIR_Modules_Surface

OBJS = ContoursToSurf.o  GenSurface.o  SurfToGeom.o
SRCS = ContoursToSurf.cc GenSurface.cc SurfToGeom.cc

LIBNAME = Surface

SO_LIBS =
SO_LDFLAGS = -L$(TOP)/lib -lDatatypes -lDataflow -lGeom -lGeometry \
	-lTCL -lClasslib -lMultitask -lMalloc -lm

SCIDependTarget($(SRCS))
SCIObjLibraryTarget($(LIBNAME),$(OBJS),$(SO_LIBS),$(SO_LDFLAGS))

#else
all:
depend::

           RM_CMD = $(RM) *.CKP *.ln *.BAK *.bak core errs ,* *~ .emacs_* tags TAGS make.log MakeOut

#endif
