
#include <config/sci.cf>

#ifdef SCI_DIR_Modules_FEM

OBJS = ApplyBC.o  BuildFEMatrix.o
SRCS = ApplyBC.cc BuildFEMatrix.cc

LIBNAME = FEM

SO_LIBS =
SO_LDFLAGS = -L$(TOP)/lib -lDatatypes -lDataflow -lClasslib \
	-lMultitask -lMalloc

SCIDependTarget($(SRCS))
SCIObjLibraryTarget($(LIBNAME),$(OBJS),$(SO_LIBS),$(SO_LDFLAGS))

#else
all:
depend::

           RM_CMD = $(RM) *.CKP *.ln *.BAK *.bak core errs ,* *~ .emacs_* tags TAGS make.log MakeOut

#endif
