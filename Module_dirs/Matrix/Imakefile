
#include <config/sci.cf>

#ifdef SCI_DIR_Modules_Matrix

OBJS = SolveMatrix.o
SRCS = SolveMatrix.cc

LIBNAME = Matrix

SCIDependTarget($(SRCS))
SCIObjLibraryTarget($(LIBNAME),$(OBJS))

#else
all:
depend:
#endif
