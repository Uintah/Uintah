#!/usr/local/bin/python

from sys import argv,exit
from helpers.runSusTests import runSusTests

TESTS = [   ("advect_mpmice", "advect.ups", "", 1),	\
            ("thermx", "thermx.ups", "", 1),     \
            ("massX", "massX.ups", "", 1),     \
            ("thermx8patch", "thermx8patch.ups", "", 8),     \
    	]

exit(runSusTests(argv, TESTS, "mpmice"))
