# -*- python -*-
# ex: set syntax=python:

# This is a sample buildmaster config file. It must be installed as
# 'master.cfg' in your buildmaster's base directory (although the filename
# can be changed with the --basedir option to 'mktap buildbot master').

# It has one job: define a dictionary named BuildmasterConfig. This
# dictionary has a variety of keys to control different aspects of the
# buildmaster. They are documented in docs/config.xhtml .


# This is the dictionary that the buildmaster pays attention to. We also use
# a shorter alias to save typing.
c = BuildmasterConfig = {}

####### BUILDSLAVES

# the 'slaves' list defines the set of allowable buildslaves. Each element is
# a tuple of bot-name and bot-password. These correspond to values given to
# the buildslave's mktap invocation.

from buildbot.buildslave import BuildSlave
c['slaves'] = [BuildSlave("DebianTesting_x86_64", "password")]

# to limit to two concurrent builds on a slave, use
#  c['slaves'] = [BuildSlave("bot1name", "bot1passwd", max_builds=2)]


# 'slavePortnum' defines the TCP port to listen on. This must match the value
# configured into the buildslaves (with their --master option)

c['slavePortnum'] = 9989

####### CHANGESOURCES

# the 'change_source' setting tells the buildmaster how it should find out
# about source code changes. Any class which implements IChangeSource can be
# put here: there are several in buildbot/changes/*.py to choose from.

from buildbot.changes.svnpoller import SVNPoller
source_code_svn_url='https://code.sci.utah.edu/svn/SCIRun/trunk'
svn_poller = SVNPoller(svnurl=source_code_svn_url,
		       pollinterval=1*60, # seconds -- every 1 minute
                       )
c['change_source'] = [ svn_poller ]



####### SCHEDULERS

## configure the Schedulers

from buildbot import scheduler

#daily = scheduler.Periodic("daily",["Uintah-Daily"],24*60*60)
#code_changed = scheduler.Scheduler(name="code_changed", branch=None,
#				   treeStableTimer=2*60, # wait 2 minutes
#				   builderNames=["Uintah-Incremental"])

quick = scheduler.Scheduler(name="quick-build", branch=None,
                            treeStableTimer=60, # wait 1 minutes
                            builderNames=["quick-linux"])

full = scheduler.Scheduler(name="full-build & test", branch=None,
                           treeStableTimer=10*60, # wait 10 minutes
			   builderNames=["full-linux"])

night = scheduler.Nightly(name='nightly', builderNames=['nightly-linux'],
             		  hour=3, minute=0)


c['schedulers'] = []
#c['schedulers'] = [ daily, code_changed]
#c['schedulers'] = [ quick, full, night]
c['schedulers'] = [ quick, full, night ]


####### BUILDERS

# the 'builders' list defines the Builders. Each one is configured with a
# dictionary, using the following keys:
#  name (required): the name used to describe this bilder
#  slavename (required): which slave to use, must appear in c['bots']
#  builddir (required): which subdirectory to run the builder in
#  factory (required): a BuildFactory to define how the build is run
#  periodicBuildTime (optional): if set, force a build every N seconds

# buildbot/process/factory.py provides several BuildFactory classes you can
# start with, which implement build processes for common targets (GNU
# autoconf projects, CPAN perl modules, etc). The factory.BuildFactory is the
# base class, and is configured with a series of BuildSteps. When the build
# is run, the appropriate buildslave is told to execute each Step in turn.

# the first BuildStep is typically responsible for obtaining a copy of the
# sources. There are source-obtaining Steps in buildbot/steps/source.py for
# CVS, SVN, and others.

from buildbot.process import factory
from buildbot.steps import source, shell


compiler_env = {'CC' : '/usr/lib/ccache/gcc',
		'CXX' : '/usr/lib/ccache/g++',
		'F77' : 'gfortran'}

rt_env = {'GOLD_STANDARD': '/usr/local/TestData'}

third_party = ["/usr/local/Thirdparty/1.25.3/Linux/gcc-4.3.1-2-64bit"]

dbg_configure_command = ["/home/buildbot/slave/uintah_quick/build/src/configure", 
                         "--enable-debug", 
                         "--enable-64bit",
		     "--enable-sci-malloc", "--enable-package=Uintah",
		     "--with-thirdparty=/usr/local/Thirdparty/1.25.3/Linux/gcc-4.3.1-2-64bit"]

opt_configure_command = ["/home/buildbot/slave/uintah_full/build/src/configure", 
                         "--enable-optimize=-O3 -mfpmath=sse", 
                         "--enable-64bit", "--enable-assertion-level=0",
                         "--disable-sci-malloc", "--enable-package=Uintah",
                         "--with-thirdparty=/usr/local/Thirdparty/1.25.3/Linux/gcc-4.3.1-2-64bit"]

#src_step = [source.SVN, svnurl = source_code_svn_url, mode="copy")

debug_f = factory.BuildFactory()
debug_f.addStep(source.SVN, svnurl=source_code_svn_url, mode="copy")

debug_f.addStep(shell.ShellCommand,
	   description=["Doing debug configure"],
           command=dbg_configure_command,
           env=compiler_env,
           workdir='build/dbg',
	   haltOnFailure=True)

debug_f.addStep(shell.ShellCommand, 
	   description=["Compiling debug build"],
           command=["make", "-j4"],
           workdir='build/dbg',
	   haltOnFailure=True)


optimize_f = factory.BuildFactory()
optimize_f.addStep(source.SVN, svnurl=source_code_svn_url, mode="copy")

optimize_f.addStep(shell.ShellCommand,
                   description=["Doing optimize configure"],
                   command=opt_configure_command,
                   env=compiler_env,
                   workdir='build/opt',
                   haltOnFailure=True)

optimize_f.addStep(shell.ShellCommand, 
                   description=["Compiling optimize build"],
                   command=["make", "-j4"],
                   workdir='build/opt',
                   haltOnFailure=True)


optimize_f.addStep(shell.ShellCommand, 
                   description=["Creating inputs symbolic link"],
                   command=["ln", "-s", 
                            "/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/StandAlone/inputs", "inputs"],
                   workdir='build/opt/Packages/Uintah/StandAlone',
                   warnOnWarnings=True)

rt_command = ["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester"]

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running Arches optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-arches", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running Example optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-e", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running Models optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-models", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running ICE optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-i", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running ICE AMR optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-ice_amr", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running MPM optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-m", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running MPMICE optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-mi", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running Implicit MPM optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-im", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')

optimize_f.addStep(shell.ShellCommand, 
                   description=["Running MPMArches optimize tests"],
                   command=["/home/buildbot/slave/uintah_full/build/src/Packages/Uintah/scripts/regression_tester", "-mpmarches", "-exact", "-opt"],
                   env=rt_env,
                   workdir='build/opt/Packages/Uintah/StandAlone')



#f1.addStep(Trial(testpath="."))

night_f = factory.BuildFactory()
night_f.addStep(source.SVN, svnurl=source_code_svn_url, mode="copy")

night_f.addStep(shell.ShellCommand,
                description=["Doing debug configure"],
                command=dbg_configure_command,
                env=compiler_env,
                workdir='build/dbg',
                haltOnFailure=True)

night_f.addStep(shell.ShellCommand, 
                description=["Compiling debug build"],
                command=["make", "-j4"],
                workdir='build/dbg',
                haltOnFailure=True)

night_f.addStep(shell.ShellCommand, 
                description=["Creating inputs symbolic link"],
                command=["ln", "-s", 
                         "/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/StandAlone/inputs", "inputs"],
                workdir='build/dbg/Packages/Uintah/StandAlone',
                warnOnWarnings=True)

night_f.addStep(shell.ShellCommand, 
                description=["Running Arches debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-arches", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running Examples debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-e", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running Models debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-models", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running ICE debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-i", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running ICE AMR debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-ice_amr", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running MPM debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-m", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running MPMICE debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-mi", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running Implicit MPM debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-im", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running MPMArches debug tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-mpmarches", "-exact", "-dbg"],
                env=rt_env,
                workdir='build/dbg/Packages/Uintah/StandAlone')



night_f.addStep(shell.ShellCommand,
                description=["Doing optimize configure"],
                command=opt_configure_command,
                env=compiler_env,
                workdir='build/opt',
                haltOnFailure=True)

night_f.addStep(shell.ShellCommand, 
                description=["Compiling optimize build"],
                command=["make", "-j4"],
                workdir='build/opt',
                haltOnFailure=True)

night_f.addStep(shell.ShellCommand, 
                description=["Creating inputs symbolic link"],
                command=["ln", "-s", 
                         "/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/StandAlone/inputs", "inputs"],
                workdir='build/opt/Packages/Uintah/StandAlone',
                warnOnWarnings=True)

night_f.addStep(shell.ShellCommand, 
                description=["Running Arches optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-arches", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running Examples optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-e", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running Models optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-models", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running ICE optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-i", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running ICE AMR optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-ice_amr", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running MPM optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-m", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running MPMICE optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-mi", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running Implicit MPM optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-im", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')

night_f.addStep(shell.ShellCommand, 
                description=["Running MPMArches optimize tests"],
                command=["/home/buildbot/slave/uintah_nightly/build/src/Packages/Uintah/scripts/regression_tester", "-mpmarches", "-exact", "-opt"],
                env=rt_env,
                workdir='build/opt/Packages/Uintah/StandAlone')




ql = {'name': "quick-linux",
      'slavename': "DebianTesting_x86_64",
      'builddir': "uintah_quick",
      'factory': debug_f,
      }

fl = {'name': "full-linux",
      'slavename': "DebianTesting_x86_64",
      'builddir': "uintah_full",
      'factory': optimize_f,
      } 

nl = {'name': "nightly-linux",
      	      'slavename': "DebianTesting_x86_64",
      	      'builddir': "uintah_nightly",
      	      'factory': night_f,
      	     } 

#c['builders'] = [quick-linux, full-linux, night-linux]
c['builders'] = [ql, fl, nl]


####### STATUS TARGETS

# 'status' is a list of Status Targets. The results of each build will be
# pushed to these targets. buildbot/status/*.py has a variety to choose from,
# including web pages, email senders, and IRC bots.

c['status'] = []

from buildbot.status import html
c['status'].append(html.WebStatus(http_port="8010",allowForce=True))

from buildbot.status import mail
c['status'].append(mail.MailNotifier(fromaddr="buildbot@retriever.sci.utah.edu",
                                     extraRecipients=["csafe-homebrew@cs.utah.edu"],
                                     sendToInterestedUsers=True,
				     lookup="sci.utah.edu"))

# from buildbot.status import words
# c['status'].append(words.IRC(host="irc.example.com", nick="bb",
#                              channels=["#example"]))
#
# from buildbot.status import client
# c['status'].append(client.PBListener(9988))


####### DEBUGGING OPTIONS

# if you set 'debugPassword', then you can connect to the buildmaster with
# the diagnostic tool in contrib/debugclient.py . From this tool, you can
# manually force builds and inject changes, which may be useful for testing
# your buildmaster without actually commiting changes to your repository (or
# before you have a functioning 'sources' set up). The debug tool uses the
# same port number as the slaves do: 'slavePortnum'.

c['debugPassword'] = "debugpassword"

# if you set 'manhole', you can ssh into the buildmaster and get an
# interactive python shell, which may be useful for debugging buildbot
# internals. It is probably only useful for buildbot developers. You can also
# use an authorized_keys file, or plain telnet.
#from buildbot import manhole
#c['manhole'] = manhole.PasswordManhole("tcp:9999:interface=127.0.0.1",
#                                       "admin", "password")


####### PROJECT IDENTITY

# the 'projectName' string will be used to describe the project that this
# buildbot is working on. For example, it is used as the title of the
# waterfall HTML page. The 'projectURL' string will be used to provide a link
# from buildbot HTML pages to your project's home page.

c['projectName'] = "Uintah"
c['projectURL'] = "http://www.csafe.utah.edu/"

# the 'buildbotURL' string should point to the location where the buildbot's
# internal web server (usually the html.Waterfall page) is visible. This
# typically uses the port number set in the Waterfall 'status' entry, but
# with an externally-visible host name which the buildbot cannot figure out
# without some help.

c['buildbotURL'] = "http://retriever.sci.utah.edu:8010/"
