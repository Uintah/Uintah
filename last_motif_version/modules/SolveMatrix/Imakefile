
#include <config/sci.cf>

LOCALINCLUDES = -I.. -I../../project
MODULELIST = ../libmodules.a

OBJS = SolveMatrix.o
SRCS = SolveMatrix.cc

.cc.o:
	$(CCC) $(CCFLAGS) -c $<

../modules.o: $(OBJS)
	cd .. && make modules.o

AllTarget(../modules.o)

SCIDependTarget($(SRCS))

