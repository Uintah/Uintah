#!/usr/local/bin/python

from sys import argv,exit
from helpers.runSusTests import runSusTests

TESTS = [   ("poisson1", "poisson1.ups", "", 1, "ALL"), \
    	]

UNUSED_TESTS = [     ("poisson2", "poisson2.ups", "", 1), \
	]

exit(runSusTests(argv, TESTS, "examples"))
