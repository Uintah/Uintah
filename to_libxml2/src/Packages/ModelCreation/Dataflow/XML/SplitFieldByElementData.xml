<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE component SYSTEM "../../../../Dataflow/XML/component.dtd">
<component name="SplitFieldByElementData" category="FieldsCreate">
  <overview>
    <authors>
      <author>Jeroen Stinstra</author>
    </authors>
    <summary>
      This module uses the data at the elements to split the mesh into mesh segements that all have the same element data.
    </summary>
    <description>
      This module uses the data at the elements to split the mesh into mesh segements that all have the same element data.  Where two elements with different values at the elements meet the nodes are split into two nodes and the elements are unlinked. The result of this module is a field in which all the elements are only linked to elements with the same value.
    </description>
  </overview>
  <implementation>
  </implementation>
  <io>
    <inputs lastportdynamic="no">
      <port>
        <name>Field</name>
        <datatype>SCIRun::Field</datatype>
      </port>
    </inputs>
    <outputs>
      <port>
        <name>SplitField</name>
        <datatype>SCIRun::Field</datatype>
      </port>
    </outputs>
  </io>
  <gui>
  </gui>
  <testing>
  </testing>
</component>

