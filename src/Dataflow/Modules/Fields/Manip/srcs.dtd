<!-- Top level -->
<!ELEMENT srcs (src+)>

<!ELEMENT src (description, lib*, libpath*, inc*)>
<!ATTLIST src name CDATA #REQUIRED>

<!-- Describe the manipulation -->
<!ELEMENT description (#PCDATA)>

<!-- gets turned into -lmyneededlib on the compile line -->
<!ELEMENT lib EMPTY>
<!ATTLIST lib name CDATA #REQUIRED>

<!-- gets turned into -L/my/needed/lib/path on the compile line -->
<!ELEMENT libpath EMPTY>
<!ATTLIST libpath name CDATA #REQUIRED>

<!-- gets turned into -I/my/needed/include/path on the compile line -->
<!ELEMENT inc EMPTY>
<!ATTLIST inc name CDATA #REQUIRED>