class Matrix
{
	double getElement(in int row, in int col);
	void setElement(in int row, in int col, in double val);
	int numOfRows();
	int numOfCols();
}


class Loader
{
	int loadComponent(in string componentType);
	int getComonents(out array<string> componentList);
}