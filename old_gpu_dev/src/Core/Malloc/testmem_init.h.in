
//
// [test]mem_init.h.in: Configure Memory Initialization Specification .h File 
//

// This file in converted by configure into mem_init.h.  It should only
// be included by new.cc and malloc.cc.  The purpose is to hold the
// Memory initialization #defines.  If the mem_init.h file is empty
// (besides this comment), that is becaues --enable-memory-initialization
// was not turned on via configure.  Otherwise the mem_init.h file should
// have just two #defines in it.

//
// Note: The mem_init.h file is placed on the binary side of the tree, eg:
//       ...SCIRun/<bin>/Core/Malloc/mem_init.h
//
// Author: J. Davison de St. Germain
// Date:   Apr 2, 2008
//

@DEF_MEMORY_INIT@
@DEF_MEMORY_INIT_NUMBER@
