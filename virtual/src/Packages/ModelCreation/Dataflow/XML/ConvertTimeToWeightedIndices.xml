<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE component SYSTEM "../../../../Dataflow/XML/component.dtd">
<component name="ConvertTimeToWeightedIndices" category="Time" optional="false">
  <overview>
    <authors>
      <author>Jeroen Stinstra</author>
    </authors>
    <summary>
      This module calculates a weights vector to do a linear interpolation between two time frames.

    </summary>
    <description>
      <p>        This module calculates a weights vector to do a linear interpolation between two time frames. This module is intended to convert results of a time adaptive simulation into a linearly interpolated time scale.

The module needs as input a vector with the time instances of each frame in the dataset and the selected time. Depending on the actual selected time a matrix with one or two weights describing a linear interpolation between the two nearest time frames is returned. This module serves as input for StreamMatrix to combine multiple computational frames into a linearly interpolate frame. 
</p>
    </description>
  </overview>
  <io>
    <inputs lastportdynamic="no">
      <port>
        <name>TimeVector</name>
        <datatype>SCIRun::Matrix</datatype>
      </port>
      <port>
        <name>Time</name>
        <datatype>SCIRun::Matrix</datatype>
      </port>
    </inputs>
    <outputs>
      <port>
        <name>TimeWeights</name>
        <datatype>SCIRun::Matrix</datatype>
      </port>
      <port>
        <name>Time</name>
        <datatype>SCIRun::Matrix</datatype>
      </port>
    </outputs>
  </io>
</component>
