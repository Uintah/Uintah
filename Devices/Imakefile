
#include <config/sci.cf>

#ifdef SCI_DIR_Devices

OBJS = Tracker.o  TrackerServer.o
SRCS = Tracker.cc TrackerServer.cc

LIBNAME = Devices

SO_LIBS =
SO_LDFLAGS = -L$(TOP)/lib -lComm -lClasslib -lMultitask -lMalloc -lC -lc

SCIDependTarget($(SRCS))
SCIObjLibraryTarget($(LIBNAME),$(OBJS),$(SO_LIBS), $(SO_LDFLAGS))

#else
all:
depend::

           RM_CMD = $(RM) *.CKP *.ln *.BAK *.bak core errs ,* *~ .emacs_* tags TAGS make.log MakeOut

#endif
