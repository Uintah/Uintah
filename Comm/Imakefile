
#include <config/sci.cf>

#ifdef SCI_DIR_Comm

OBJS = MessageBase.o
SRCS = MessageBase.cc

LIBNAME = Comm

SCIDependTarget($(SRCS))
SCINormalLibraryTarget($(LIBNAME),$(OBJS))

#else
all:
depend:
#endif

